import tw from "twin.macro";

type Props = {};

const Container = tw.div`min-h-[calc(100vh-3.24rem)] grid place-items-center text-3xl`;

const ProductPage = (props: Props) => {
	return <Container>ProductPage</Container>;
};
export default ProductPage;
